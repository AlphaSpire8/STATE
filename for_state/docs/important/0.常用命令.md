## 环境配置
cd /data3/fanpeishan/state && conda activate vcc && source .venv/bin/activate && clear

## 设置CUDA_VISIBLE_DEVICES环境变量
export CUDA_VISIBLE_DEVICES=1

## git操作
1. 创建并提交分支
   - 创建新分支并取名
   - 暂存所有更改
   - 添加提交信息
   - 提交
2. 切换到主分支
3. 合并特性分支（保留合并历史）
   - git merge new-feature --no-ff -m "merge new feature"
4. 删除分支
5. 同步到GitHub